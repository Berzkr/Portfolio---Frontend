export class NuevoUsuario {

    //el "!" se utiliza para ignorar el error que nos tira por no estar inicializada la variable
    nombre!: string;
    nombreUsuario!: string;
    email!: string;
    password!: string;
    authorities!: string[];

}
